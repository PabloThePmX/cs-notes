# Aula 6

* Para usar o KNN, usar a lib `KNNImputer` do `sklearn.impute`.
* O histograma é usado para ver a distribuição de frequência de dados quantitativos.
* O boxplot é usado para resumir a distribuição utilizando quartis.
  * Bom para quando tem outliers.
  * Cada retângulo equivale aos valores dentro do desvio padrão.