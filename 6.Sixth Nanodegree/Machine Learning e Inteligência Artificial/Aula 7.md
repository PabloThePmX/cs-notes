# Aula 7

* ETL.
* `z-score` e `min-max scaling`.
  * O scaling é uma formula.
* A normalização padrão é entre 0 e 1.
* Usar `drop_duplicates` para remover dados duplicados.
* Outliers: Registros realmente extremos que refletem parte da realidade.
  * Identificação e remoção.
  * `IQR`.
* O outlier costuma ser removido, mas ele pode também ser válido em casos específicos.